var addressFormTest = require('./address-form-test');
var paymentFormTest = require('./payment-form-test');

module.exports = {
    'Test staging store purchase address form': addressFormTest,
    'Test staging store purchase checkout payment page': paymentFormTest
};